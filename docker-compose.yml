version: '3.8'

services:
  emotion-classifier:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=False
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=5000
    volumes:
      - .:/app
    restart: unless-stopped